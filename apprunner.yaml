version: 1.0
runtime: python3.11
build:
  commands:
  
    build:
      - pip3 install  -r requirements.txt
      - pip3 install --upgrade pip
run:
  command: gunicorn app:app  
  network:
    port: 8000