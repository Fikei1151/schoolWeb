version: 1.0
runtime: python311
build:
  commands:
  
    build:
      - pip3 install --no-cache-dir -r requirements.txt
      - pip install --upgrade pip
run:
  command: gunicorn app:app  
  network:
    port: 8000